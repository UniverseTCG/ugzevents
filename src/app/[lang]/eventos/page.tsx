import{notFound}from'next/navigation';import{getDictionary,hasLocale}from'../../../dictionaries';
export default async function Page({params}:{params:Promise<{lang:string}>}){const{lang}=await params;if(!hasLocale(lang))notFound();const d=(await getDictionary(lang)).events;return <div className="container" style={{padding:'4rem 2rem'}}><h1>{d.title}</h1><p>{d.intro}</p><div className="grid grid-cols-2">{[1,2,3,4].map(i=><div key={i} className="card"><h2>{d.league} {i}</h2><p>{d.pending}</p><a href="https://weezevent.com" className="btn btn-primary">{d.register}</a></div>)}</div></div>}
