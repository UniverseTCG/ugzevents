import type{Metadata}from'next';import{notFound}from'next/navigation';import{getDictionary,hasLocale}from'../../../dictionaries';
export async function generateMetadata({params}:{params:Promise<{lang:string}>}):Promise<Metadata>{const{lang}=await params;if(!hasLocale(lang))notFound();const d=(await getDictionary(lang)).meta;return{title:d.live,description:d.liveDesc}}
export default async function Page({params}:{params:Promise<{lang:string}>}){const{lang}=await params;if(!hasLocale(lang))notFound();const d=(await getDictionary(lang)).live;return <div style={{padding:'4rem 2rem',background:'var(--bg-secondary)'}}><section className="container" style={{textAlign:'center'}}><div className="pill">● {d.badge}</div><h1>{d.title}</h1><p>{d.intro}</p><div className="card" style={{aspectRatio:'16/9',background:'#000',color:'white',display:'grid',placeItems:'center',margin:'3rem auto'}}><div><h2 style={{color:'white'}}>{d.offline}</h2><p>{d.starts}</p></div></div><h2>{d.guests}</h2><div className="grid grid-cols-3">{d.people.map((x,i)=><div className="card" key={i}><h3>{x[0]}</h3><p>{x[1]}</p></div>)}</div></section></div>}
