import{notFound}from'next/navigation';import{getDictionary,hasLocale}from'../../../dictionaries';
export default async function Page({params}:{params:Promise<{lang:string}>}){const{lang}=await params;if(!hasLocale(lang))notFound();const d=(await getDictionary(lang)).refund;return <div style={{padding:'4rem 2rem',background:'#f1f5f9'}}><article className="container" style={{maxWidth:900,background:'white',padding:'4rem',borderRadius:16}}><h1>{d.title}</h1><div style={{padding:'1.5rem',background:'#FEF2F2',borderLeft:'6px solid #E3350D'}}><strong>{d.notice}</strong></div>{d.sections.map((x,i)=><section key={i}><h2>{x[0]}</h2><p>{x[1]}</p></section>)}</article></div>}
