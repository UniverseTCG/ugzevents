export default function NewsTicker({alerts}:{alerts:string[]}){const repeated=[...alerts,...alerts,...alerts];return <div className="news-ticker-container"><div className="news-ticker-content">{repeated.map((alert,i)=><div key={i} className="news-ticker-item">{alert}<span style={{margin:'0 2rem',color:'var(--poke-yellow)'}}>•</span></div>)}</div></div>}
